<!-- Shared block: browser-based course calendar (xeus-lite).
     Host page selects a course into `c` (e.g. c = courses.data8) before including. -->
The full course calendar is also available as an in-browser notebook preview, with links
to slides, worksheets, and videos where available. It runs the notebooks directly in your
browser (xeus-lite), so it is handy for previewing the materials. It is not meant for
active classroom use, but it is a quick way to explore what the course covers.

[Browse {{ c.title }} notebooks in your browser]({{ c.xeus_lite }})
